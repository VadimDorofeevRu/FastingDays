<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/previous_year"
        android:title="@string/previous_year"
        android:contentDescription="@string/previous_year"
        android:icon="@drawable/left"
        android:iconTint="?attr/navBg"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction" />
    <item
        android:id="@+id/next_year"
        android:title="@string/next_year"
        android:contentDescription="@string/previous_year"
        android:icon="@drawable/right"
        android:iconTint="?attr/navBg"
        app:showAsAction="always"/>
</menu>